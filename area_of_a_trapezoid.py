print("Area of a Trapezoid")
height = float(input("enter height of trapezoid:"))
bottom_length = float(input("enter length of bottom base:"))
top_length = float(input("enter length of top base:"))
A = 1/2 * (bottom_length + top_length) * height
print("The area is:", A)